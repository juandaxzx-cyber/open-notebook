Classify the following study topic along three axes, using the learner's
retrieved material as context.

Topic: {{topic}}

Material excerpts:
{{content}}

Axes:
- verifiability: "verifiable" (answers can be checked objectively) or "interpretive"
- structure: "hierarchical" (concepts build on prerequisites) or "distributed" (a web of loosely ordered ideas)
- production: "recall" (remember facts/terms), "apply" (solve/execute), or "explain" (explain, argue, transfer)

Answer with ONLY a JSON object, no prose:
{"verifiability": "...", "structure": "...", "production": "..."}
